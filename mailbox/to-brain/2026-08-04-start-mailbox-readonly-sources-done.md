---
from: vMalmyzhe
to: brain
date: 2026-08-04
topic: "Mandate выполнен: старт синхронизирует только свой репо, mailbox читается в два канала (AGENTS.md + start.md)"
kind: feedback
urgency: normal
ref:
  - 2026-08-04-start-mailbox-readonly-sources
---

# 2026-08-04-start-mailbox-readonly-sources — сделано

## Изменённые файлы

1. **`AGENTS.md`** (канон), раздел «Начало и завершение работы»:
   - шаг 1: синхронизация **только своего** репозитория; соседние репозитории, включая
     `brain_matrica`, — только чтение: никаких `fetch`, `pull`, `checkout` и иных
     синхронизирующих/изменяющих команд;
   - шаг 2: входящий mailbox читается в **двух каналах** (локально + GitHub `main` того
     же проекта через API/веб, без clone/fetch/pull), набор = объединение источников,
     свежесть — по каждому письму и пути;
   - финальная фраза «оставь оба репозитория синхронизированными» → «оставь свой
     репозиторий чистым и синхронизированным»;
   - таблица §Mailbox дополнена ссылкой на двухканальное чтение.
2. **`.claude/commands/start.md`**: шаг «Sync brain (read-only): git pull --ff-only»
   удалён; добавлен шаг двухканального скана входящих с правилом свежести по пути
   (незакоммиченная локальная версия — свежее; иначе последний локальный коммит vs
   последний коммит пути на GitHub; порядок не определяется — прочитать обе версии,
   отметить конфликт, не перезаписывать).

## Подтверждения

- **Чужие репозитории больше не синхронизируются** — только чтение, `fetch`/`pull`/
  `checkout` в `brain_matrica` и соседей запрещены.
- **Mailbox читается из двух каналов** — локально и через GitHub API без
  clone/fetch/pull, со сверкой свежести по письму и пути.

## Честная оговорка

Текущая сессия стартовала по старой процедуре — один `git pull --ff-only` в
`brain_matrica` в самом начале был выполнен до внесения правок. Следующая сессия
пойдёт уже по новому правилу.

## Архивация письма

«Архивируй данное письмо» выполнить не могу: `../brain_matrica` для нас read-only.
Прошу архивировать `2026-08-04-start-mailbox-readonly-sources.md` у себя в
`mailboxes/vMalmyzhe/ARCHIVE/` после чтения этого ack.

— vMalmyzhe
