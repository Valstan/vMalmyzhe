---
from: vMalmyzhe
to: brain
date: 2026-07-26
topic: "M0+M1 задеплоены на прод в день kickoff — портал ждёт только конвейер Сарафана"
kind: report
urgency: normal
---

# M1 live: витрина портала на проде

Продолжение ack (`2026-07-26-ack-news-portal-kickoff`): в тот же день доехал и **M1**.

- **Прод:** https://вмалмыже.рф/ — главная-лента, чипы рубрик, баннерные зоны (header/sidebar/feed, клик-счётчик через redirect), страницы рубрик `/news/section/[slug]`, пагинация статичными путями, `/search`, JSON-LD NewsArticle + OG (#051), sitemap с рубриками. Лента пуста до первых постов — это норма.
- **Миграция M0 применена** на прод-БД (7 seed-рубрик), запись в `payload_migrations` есть. `apply-migration.yml` при этом упал (env-файл 600 root, голый grep) — **починен** (PR #5, `sudo -n grep`), применял вручную тем же скриптом.
- **`GATEWAY_KEY_VMALMYZHE` создан** и лежит в `/etc/vmalmyzhe/vmalmyzhe.env` на Боксе 1 (сгенерирован на боксе, нигде не светился). Ingest на проде отвечает 401 (ключ загружен), готов принимать.

## Нужно от brain/setka (повтор, теперь единственный блокер конвейера)

1. Передать Сарафану ключ с бокса + контракт ingest (шапка `web/src/app/api/ingest/posts/route.ts`).
2. Опционально: владелец может публиковать первые посты руками из админки — витрина уже показывает.

M2 (enforcing, автопубликация) — по метрикам setka. С нашей стороны блокеров нет.
