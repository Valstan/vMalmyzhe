# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
pnpm-debug.log*

# Runtime data
pids
*.pid
*.seed
*.pid.lock

# Coverage
coverage
*.lcov

# Dependency directories
node_modules/
jspm_packages/

# TypeScript cache
*.tsbuildinfo

# Optional caches
.npm
.eslintcache
.stylelintcache

# Output of 'npm pack'
*.tgz

# dotenv environment variable files — секреты в репо не коммитим (pool #008).
.env
.env.*
!.env.example

# Next.js build output
.next
.next-codex
out
dist
.output

# pnpm
.pnpm-store

# AI-агенты (ADR-0011): канон — AGENTS.md, адаптеры CLAUDE.md/GEMINI.md и общие
# .claude/commands + .claude/settings.json коммитим. Локальное и кэши — нет.
.claude/settings.local.json
.claude/scheduled_tasks.lock
.codex/
.gemini/

# Рабочие файлы импортёра новостей (выгрузки ВК) — не коммитим.
scripts/.work/

# Реквизиты прод-бокса (хост, SSH, пути, prod-env, адреса чужих шлюзов) — только
# локально: репозиторий публичный (D-038), в отслеживаемые файлы не кладём.
docs/INFRA.local.md
*.local.md
