# вМалмыже — Session Handoff

> Sticky-note для непрерывности сессий. Перезаписывается `/close_session`. История — `git log -- docs/SESSION_HANDOFF.md`.

**Status:** ACTIVE — портал наполнен живым контентом из ВК, аналитика включена, vault-контур замкнут, гейты настоящие (CI на PR + тесты + **сканер секретов**), репозиторий **публичный** с 17.08 и вычищен от инфра-деталей (D-038)
**Updated:** 2026-08-23 (сессия: почтовый долг 15–23.08 — сканер секретов в `gates` с красной приёмкой, обход D-038 и переписанный предохранитель, 4 письма в brain; PR #44 смержен, #45 — одноразовая красная приёмка, #46 — D-038, #47 — favicon; прод на `f6a418d`)

## Что сделано (2026-08-23)

Разобран почтовый долг с 15.08: **7 новых писем от brain** (2 mandate high,
2 mandate, 3 recommend). Главное — репозиторий стал **публичным 17.08**
(D-030), `main` защищён сервером, обязательная проверка — `gates`.

- **Сканер секретов стоит** (мандат 17.08, срок 24.08) — PR #44: шаг
  `Secret scan (gitleaks)` **внутри `gates`** сразу после `checkout@v5` с
  `fetch-depth: 0`; `gitleaks-action@v3` с пином `GITLEAKS_VERSION: 8.30.1`
  (без пина action ставит зашитый 8.24.3). Конфиг `.gitleaks.toml`: дефолт +
  4 своих правила — `vk-access-token`, `telegram-bot-token-anywhere` (дефолтное
  пропускает токен в URL; ⚠️ с ведущим `` своё тоже не ловило URL —
  между `bot` и цифрой нет границы слова), `vmalmyzhe-env-secret` (значение
  рядом с именем нашего секрета, без порога энтропии), `postgres-uri-with-password`.
  Ложных срабатываний на дереве — 0.
  - **Приёмка #114 красным** — PR #45 (одноразовая ветка, закрыт, удалена):
    run `32635116684`: `4 commits scanned`, шаг → failure,
    `leaks found: 3`, все `Secret: REDACTED`. Классы: VK, TG-в-URL, generic.
  - `.gitleaks.toml` добавлен в `paths-ignore` деплоя.
- **D-038 обход выполнен, предохранитель переписан** — PR #46. Нашлось в
  11 живых файлах (6 категорий): хост+хостер+«Бокс 1» в каноне; SSH-логин
  и абсолютные пути в обоих workflow и юните; путь prod-env в `.env.example`,
  комментариях кода, инструкции; SSH-алиас в `PUBLISHING.md`/памятке/техзаметке;
  **хост чужого бокса** (шлюз Сетки) литералом в `vk-fetch.mjs`; `hostname`
  печатался в публичный лог CI (`apply-migration.yml`).
  - Логин → `vars.DEPLOY_SSH_USER`, корень релизов → `$HOME`, путь prod-env →
    `vars.PROD_ENV_FILE`; юнит — шаблон с `__DEPLOY_USER__`/`__PROD_ENV__`;
    шлюз Сетки — `SARAFAN_GATEWAY_URL` (лежит в `web/.env`).
  - ⚠️ **`gh variable set … --body /etc/…` из Git Bash записал
    `C:/Program Files/Git/etc/…`** (MSYS подменяет POSIX-пути). Поймано
    `gh variable get`, перезаписано с `MSYS_NO_PATHCONV=1`. Проверять значения.
  - **Реквизиты бокса — только в `docs/INFRA.local.md`** (в `.gitignore`).
    Новый раздел `AGENTS.md` «Публичный репозиторий — тоже recon-поверхность».
  - Датированные записи handoff и письма не правились (история, D-038).
- **#171 (vault one-shot)** — мины нет: приложение в комнату не ходит, prod-env
  — источник истины, комната держит зеркало. Письмо отправлено.
- **ЕСА (мандат 26.07, проверка brain 01.09)** — клиент **не выдан**: в prod-env
  нет ESA/OIDC-переменных, ответов Сарафана нет. Письмо-запрос brain: выдача
  `client_id`/`client_secret` (redirect `https://вмалмыже.рф/api/auth/callback`,
  PKCE S256) либо времянка владельца. Код клиента до выдачи не пишем.
- **G260** (сиды/дампы в учёте) — чисто; `scripts/.work/` в истории — 0 коммитов.
- **Favicon на 16 px — посмотрен глазами и перегенерирован** (пункт 4 письма
  brain 15.08, #093). Диагноз по пиксельной карте 16-px кадра ICO: ястреб
  занимал 7 строк из 16 (растянут на всю ширину с пустыми полями сверху/снизу),
  в 1:1 — нечитаемая чёрточка; плюс мусор от обрезки источника — золотая полоса
  с фигуркой в левом верхнем углу и серые линии по обоим краям. Сделано (PIL):
  вырезана зона птицы (y ≥ 150, 12 ≤ x ≤ 500 в 512-px исходнике), квадрат с
  полем 6 %, Lanczos; для малых кадров — утолщение штриха `MinFilter` на
  8× промежуточном (16 px — 5, 32 px — 7, 48 — без): при 15 крылья сливались в
  кляксу, при 0 — бледная чёрточка. Проверено рендером вариантов ×10.
  Скриншоты вкладки не снимались — вкладка Chrome-расширением не снимается,
  встроенная панель без глаз владельца кадры не рисует; смотрел извлечённые
  кадры ICO через image-reader, как и предписывает #093.
- Гейты локально зелёные: lint, typecheck, **32 теста**, deadcode; YAML ×3.
- **4 письма в brain** (сканер+#170+G260 · D-038 · #171 · ЕСА-запрос), все с `ref`.
- ⚠️ Сессия пережила сброс лимита: часть работы (PR #44/#45, переменные репо)
  выпала из контекста модели и была восстановлена по `git reflog`, `gh pr list`,
  `gh run view`. Состояние в Git — источник истины, чат — нет (канон §Git).

**Branch:** main (всё смержено, #44–#47; локально чисто, сессия закрыта 23.08 ~18:15)

## Что сделано (2026-08-10)

Разобран почтовый долг: **7 новых писем от brain**, из них 4 мандата. Три из
четырёх оказались фактически выполненными ещё 03.08 — brain об этом не знал,
потому что отчёты уходили абзацем внутри общего отчёта сессии, а не письмом на
слаг мандата. **Правило изменено: один мандат — одно письмо с `ref` на его
слаг**, даже если работа сделана попутно (это же и грабля из письма
`2026-08-08-ref-must-be-the-letter-slug`).

- **Аналитика включена.** Счётчик Метрики заводить не пришлось: в аккаунте
  владельца он уже был — «Вмалмыже», `вмалмыже.рф`, **47115810**, с нулями и
  без кода на сайте. Номер прописан в прод-env, сервис перезапущен. Приёмка со
  стороны Метрики: `POST mc.yandex.ru/watch/47115810` → 200, в кабинете
  1 визит / 1 посетитель / 1 просмотр. **В кабинете владельца 7 счётчиков —
  перед заведением нового всегда смотреть список.**
  - В подвал добавлен **штатный информер Яндекса** (88×31): видимая цифра —
    требование владельца (D-017), и она не должна ждать OAuth-токена, которого
    нет. Наш блок «сегодня / вчера» в тот же день **отменён владельцем** и
    удалён — см. раздел ниже.
  - Строка уведомления об обработке данных — там же, со ссылкой на политику
    Яндекса. Отдельной страницы политики нет: состав страниц — решение владельца.
  - G136 не взведён: «принимать данные только с указанных адресов» выключено,
    молча резать визиты нечему.
- **G209 проверен — чисто.** Обе JSON-LD-поверхности экранировали `<` и до
  проверки. Экранирование вынесено в `web/src/lib/jsonLd.ts` (было копипастой
  в двух местах), добавлены 4 теста **на строку, уходящую в `__html`**, включая
  демонстрацию самой ловушки. Найдена третья поверхность того же класса — номер
  счётчика внутри инлайнового `<script>`; закрыт валидацией `^\d+$`.
- **G234: keep-alive задан, drain отложен.** `KEEP_ALIVE_TIMEOUT=65000` в
  прод-юните (было — дефолт Node 5 с). Гонка у нас **не воспроизводима**: в
  nginx нет `upstream{keepalive}`, пул к приложению не держится. SIGTERM-drain
  осознанно отложен (требует обёртки над сгенерированным `server.js`); триггер
  возврата — серийная доставка Сарафана по расписанию или появление
  upstream-пула.
  - ⚠️ `headersTimeout` Next-standalone наружу не отдаёт: он остаётся 60 000 мс
    и ограничивает idle сверху. Пара 65 000 / 66 000 без своей обёртки
    недостижима — пул прокси надо держать короче 60 с.
- **Plaintext-копия токена комнаты: удалять нечего.** Обысканы рабочее дерево,
  `web/.env`, Documents/Desktop/Downloads/brain_infra, скретчпады сессий, обе
  истории shell на машине владельца; на Боксе 1 — `/etc/vmalmyzhe/*`, `/root`,
  домашний каталог, `/tmp`, `/var/tmp` и обе `.bash_history`. Строку
  `SECRETS_TOKEN` содержит **только** рабочий `vmalmyzhe.env`. Две копии на
  стороне владельца он стирает сам.
  - Найден мусор: `/etc/vmalmyzhe/.env.u1xhye` — **пустой файл** от 03.08,
    остаток атомарной записи env. **Удалён 10.08 по решению владельца** (перед
    удалением подтверждён нулевой размер).
- Гейты локально зелёные: lint, typecheck, **32 теста** (было 25), deadcode.
  `build` не гонялся (нет Postgres) — собрал `deploy-prod.yml`.
- **PR #40, #41, #42 смержены, оба деплоя зелёные**, прод проверен глазами
  после каждого: в подвале строка «ПОСЕЩАЕМОСТЬ · информер 88×31 · Сайт
  использует Яндекс.Метрику — политика конфиденциальности», одна строка 39 px,
  три элемента; блока «Сегодня / Вчера» в HTML нет. Подпись автора на месте.
  На информере к концу сессии 4 просмотра / 2 визита / 1 посетитель — свои же
  заходы, но сбор рабочий.
- **6 писем в brain** (по одному на мандат + постскриптум + отмена блока), все
  с `ref` на full-slug исходного письма.

**Branch:** main (всё смержено; локально чисто)

### Блок «сегодня / вчера» — закрыт решением владельца 10.08

**Не делаем.** В подвале остаётся только штатный информер Метрики.

Владелец рассчитывал, что цифры динамики (сегодня / вчера / неделя / месяц —
как на длинном счётчике ЛайвИнтернета) приходят вместе со счётчиком. Проверено
в кабинете: **настройка информера — это одна галочка, без выбора периода**, и
информер отдаёт только сегодняшние три числа (просмотры, визиты, уникальные
посетители). Любой другой период — Reporting API, то есть отдельный
OAuth-токен, который надо выпускать и потом продлевать. Владелец решил, что
ради строки в подвале это не окупается.

Серверный код блока удалён (`getMetrikaStats`, тип `MetrikaStats`, проп
`stats`, переменная `YANDEX_METRIKA_TOKEN` в `.env.example`). Восстанавливать
заново не нужно, если решение переиграют: `git log -- web/src/lib/metrika.ts`.

⚠️ Уточнение, если вопрос вернётся: блок стоил **ноль** клиентской нагрузки —
запрос делал сервер, в браузер уезжали готовые числа. Отказ был из-за возни с
токеном, а не из-за веса страниц; **картиночный информер, наоборот, добавляет
по одному внешнему запросу на страницу.**

## Что сделано (2026-08-05, поздний вечер)

- **Фавиконка с официального герба города** (чёрный ястреб, Wikimedia Commons, файл
  `Coat of Arms of Malmyzh.png`): силуэт ястреба вырезан программно (порог по
  яркости + связные компоненты), посажен на белый фон. Файлы в
  `web/src/app/`: `favicon.ico` (16/32/48 px, PNG-вложенные — контейнер валиден),
  `icon.png` (512), `apple-icon.png` (180). Next.js подхватывает автоматически.
  **PR #38**, смержен, деплой идёт.
- ⚠️ Модель не видит картинки — силуэт проверялся ASCII-превью 32px (птица с
  распростёртыми крыльями читается), но глазом на 16px не проверен. При
  первом удобном случае глянуть вкладку браузера: если на малых размерах
  сливается — упростить (толще крылья, чище хвост).
- Гейты локально зелёные: lint, typecheck, 25 тестов. `build` не гонялся (нет
  Postgres) — соберёт `deploy-prod.yml`.

**Branch:** main (всё смержено; локально чисто)

## Что сделано (2026-08-05, вечер)

- **Ревизия гейтов #104 закрыта полностью** (PR #35):
  - 4 собственных lint-правила подняты `warn` → `error` (в warn next lint
    возвращал 0 — как гейт были пустышкой). Сгенерированные Payload-миграции
    осознанно исключены из `no-unused-vars` (клиент генерирует сигнатуры
    `up({db,payload,req})` с полными аргументами). `no-img-element` —
    файловые disable по конвенции PostList.tsx (галерея и инлайн-медиа ВК не
    переводятся на next/image).
  - **knip включён в CI** как гейт `deadcode` — был скрипт, который никто не
    вызывал. Почищены найденные unused exports: `MAX_VIDEOS`/`textNode`/
    `paragraph`/`videoParagraph`/`imageNode` (внутренние хелперы ingest.ts),
    `PostCard` (только внутри PostList.tsx), тип `DayStats` (внутри metrika.ts).
  - **Красные прогоны показаны (#104)**: lint — нарочный `any` → exit 1;
    deadcode — нарочный unused-export → exit 1; оба откачены.
  - CI на PR и на main — зелёный со всеми 5 гейтами, деплой прошёл, прод 200.
- **Статистика рубрик** (для решения владельца о финальном списке): Культура и
  спорт 5, Новости 4, Афиша 4, Объявления 2, ЖКХ 2, История Малмыжа 1,
  Происшествия 1 — все 7 рубрик наполнены, ни одна не выдумывалась.

**Branch:** main (всё смержено; локально чисто)

## Что сделано (2026-08-05, утро)

- **Фича «медиа внутри текста» доехала до прод**: картинки поста встраиваются
  между абзацами (узел `upload`, схема Payload 3), клик открывает лайтбокс-галерею
  с пролистыванием, видео ВК рендерится встроенным плеером `video_ext.php`.
- **2 бага убиты по пути**: upload-узел собирался в неверной схеме
  (`fields.{relationTo,value}`, version 2 — реальная: поля на уровне узла,
  `version: 3`, обязательный `id`), из-за чего **7 из 9 постов падали на проде**
  `ValidationError: 'Текст новости' invalid` → HTTP 500. Затем при population
  Payload подставляет в `value` объект медиа, а не id — картинки не рендерились.
  Оба закрыты PR #32 и #33, добавлен `web/src/lib/RichText.test.ts` (7 тестов).
- **Импортировано и опубликовано 9 новостей 05.08** (`selection-2026-08-05.json`):
  впервые заполнена рубрика **«Происшествия»** (#77573, порча клумб на стадионе),
  портал теперь 19 постов в 7 рубриках (было 10 в 6). Отбор и разметка — по
  `docs/PUBLISHING.md`. Позитивный путь гейта «верный ключ публикует» пройден
  вживую на реальном импорте.
- **Приёмка на проде**: каждый пост 200, `post-inline-img` = фото-1 (первое ушло
  в обложку), видео-плеер = 1, старые посты рендерят хвостовую галерею как раньше.
- Прод-временка `/tmp/vkimport` почищена после заливки.

**Branch:** main (всё смержено; локально чисто)

## Текущая нитка

**Портал перестал быть пустым каркасом.** На проде 10 опубликованных новостей в 6 рубриках, взятых из сообщества «МАЛМЫЖ - ИНФО» (`malmig_info`, owner_id `-158787639`) через VK-шлюз Сарафана. В шапке — кнопка «Сервисы вМалмыже.рф».

## Что сделано (2026-08-03)

- **Vault-онбординг закрыт:** выданный КАРМАНом `SECRETS_TOKEN` установлен в prod-env
  Бокса 1 без вывода значения; `GATEWAY_KEY_VMALMYZHE` зеркалирован в комнату
  `vmalmyzhe`. Приёмка: GET 200 → POST 200 → контрольный GET 200, возвращённое
  значение совпало с источником. Права prod-env остались `root:root 0600`.

- **PR #14** — кнопка каталога сервисов в шапке (п.3 `SERVICE_ONBOARDING`, mandate 07-26; триггер «при наполнении контентом» наступил). Ведёт на `вход.вмалмыже.рф/services` в punycode. Проверена на 1280 / 1000 / 390 px: переполнения нет, на мобильном разворачивается во всю ширину под названием.
- **PR #14** — ingest дополнен `publishedAt`, `videos`, `publish`.
- **PR #15** — фикс: `publish` обязан ставить `_status`, иначе пост остаётся черновиком.
- **Импортировано и опубликовано 10 новостей** датами оригинальных постов ВК (не сегодняшними).
- **PR #17 + #20** — подпись автора в подвале: «Сделано с заботой о нашем районе. (с) **Студия «Матрица» - Валентин Савиных**, 2026», выделенное — ссылка на `валентин.вмалмыже.рф` (п.4 онбординга, mandate 08-01; формулировка по заказу владельца). Год динамический. Ссылке нужен явный `display: inline`: общее правило `.site-footer a` задаёт `block`.
- **Заявка в каталог сервисов** (п.2) отправлена письмом в brain.
- **ADR-0011 выполнен**: `AGENTS.md` — единственный канон, `CLAUDE.md` (9 строк) и `GEMINI.md` — тонкие адаптеры, `.gitignore` дополнен `.codex/`/`.gemini/`. Проверка 4 из поправки 07-31 сработала: `/start` ссылался на `CLAUDE.md`, после сжатия грузил бы настройки инструмента вместо правил — ссылка переведена на `AGENTS.md`.

### Мандаты онбординга закрыты

| Пункт | Статус |
|---|---|
| п.1 вход через ЕСА | ⛔ не построен — упирается в регистрацию клиента (класс ② ADR-0010, проблема первого доступа #116) |
| п.2 запись в каталог | ✅ заявка отправлена |
| п.3 кнопка «Сервисы» | ✅ живая |
| п.4 подпись автора | ✅ живая |

### Ключ VK-шлюза — блокера не было

Реестр Мозга показывал нас только приёмником, из чего читалось «консьюмерского ключа нет» (письмо brain 08-01 «ты заперт»). **По факту ключ `VMALMYZHE` в БД `gateway_keys` Сетки живёт с 2026-07-13** и ни разу не использовался. В сессии он ротирован с разового разрешения владельца (граница ADR-0009, прецедент setka 31.07), значение — в `web/.env` как `SARAFAN_GATEWAY_KEY`, в репо не попадает.

⚠️ **Имя `GATEWAY_KEY_VMALMYZHE` означает два разных ключа**: у нас — приёмника (Сарафан авторизуется у нас), у Сетки — потребителя (мы авторизуемся у неё). Разведены в `web/.env.example`.

### G211 проверен — и вскрыл дыру, открытую этим же днём

`Where` возвращают только `adminOrSelf` и `authenticatedOrPublished`, ни одна не стоит на `create` — классическая грабля нас не задевает. Но флаг `publish`, добавленный утром, снял конструктивную защиту «всегда draft»: держатель ключа доставки мог публиковать мимо ревью (#107). **Право публиковать отделено в отдельный секрет** `INGEST_PUBLISH_KEY` (заголовок `X-Publish-Key`, PR #21); без него — черновик + warning. Значение живёт в прод-env, бэкап env рядом.

⚠️ Позитивный путь гейта (верный ключ → публикуется) закрыт на реальном импорте
05.08: 9 постов, всем `published=true`. Режим отказа (без `INGEST_PUBLISH_KEY`
→ черновик + warning) — безопасная деградация по замыслу.

### Публикация, аналитика, видимость в нейросетях (PR #23)

- **[`docs/PUBLISHING.md`](PUBLISHING.md) — рабочая инструкция публикации** + `scripts/vk-fetch.mjs`, `scripts/vk-import.mjs`, `scripts/selection.example.json` (реальный отбор 03.08). Выгрузчик помечает `⚠СБОРНЫЙ`; импортёр требует явные индексы медиа и падает на несуществующем — «все подряд» технически невозможно.
- **Яндекс.Метрика**, ЛайвИнтернет намеренно не ставим. Информер в подвале — «сегодня / вчера», цифры берутся Reporting API **на сервере** (токен в браузер не уезжает), кеш 10 минут. ⚠️ Отменено 10.08: остался только штатный информер Яндекса, см. раздел за 10.08.
- **`/llms.txt`** и **`/rss.xml`** — живые, проверены на проде. Разметка: `NewsMediaOrganization` + `WebSite` на каждой странице; у статьи `description`, `author`, `inLanguage`, `isBasedOn` (ссылка на пост ВК машиночитаемо), `contentLocation` (привязка к Малмыжу).

✅ Закрыто 10.08: `NEXT_PUBLIC_YANDEX_METRIKA_ID=47115810` стоит в прод-env, счётчик принимает данные. `YANDEX_METRIKA_TOKEN` не нужен — блок «сегодня / вчера» отменён владельцем.

### Ревизия гейтов #104 — вскрыла отсутствие контура, а не дыру в правиле (PR #25)

**Автоматических гейтов на PR не было вообще.** `lint`/`typecheck` гонял вручную агент; единственная машинная проверка (`next build`) стояла в деплое — уже **после** мержа в `main`. «Гейты зелёные» означало «агент вспомнил запустить». Заведён **`ci.yml`**: `lint`, `typecheck`, `test` на каждом PR.

**Тестов поведения не было ни одного** — поэтому оба сегодняшних бага доехали до прода через законно зелёный контур. Чистая логика ingest вынесена в `web/src/lib/ingest.ts`, поверх неё **14 тестов** по следам этих багов.

Красные прогоны показаны: `test` → exit 1 (вернули баг `_status`), `typecheck` → exit 2, `lint` → exit 1 (rules-of-hooks). `eslint-plugin-react-hooks` подключён — класс, на котором сгорела Матрица, ловится.

⚠️ **Зелёные тесты не заменяют typecheck**: vitest транспилирует без проверки типов. Поймано вживую — `tsc` был красным полчаса, пока `pnpm test` рапортовал зелено.

**Осталось нетронутым сознательно:** все четыре собственных lint-правила стоят в `warn`, а `next lint` на warning'ах возвращает 0 — как гейт они бесполезны. Включение на живом коде идёт **отдельной веткой** (п.5 мандата). `knip` (`pnpm deadcode`) не вызывается нигде — пустышка, решить: в CI или убрать.

## Следующий шаг

1. ~~**Подключить Метрику**~~ — закрыто 10.08 полностью: счётчик 47115810
   живой, визиты идут, информер в подвале. Блок «сегодня / вчера» отменён
   решением владельца, хвостов по аналитике не осталось.
2. **Регулярный ритм наполнения** — инструмент готов ([`docs/PUBLISHING.md`](PUBLISHING.md)), два живых импорта отработаны (03.08, 05.08). Отбор остаётся ручным по замыслу. Решить, кто ведёт ежедневный отбор.
3. **Заменить демонстрационные блоки данными** — реальные предприятия и ИП, рекламные размещения.
4. ~~Поднять lint-правила в error, решить судьбу knip~~ — закрыто 05.08 (PR #35): lint в error, knip в CI.
5. **SIGTERM-drain** (G234, отложен 10.08) — каждый мерж в `main` рвёт запросы
   в полёте. Триггер возврата: серийная доставка Сарафана по расписанию (D-015)
   или появление `upstream{keepalive}` в nginx. Требует обёртки над
   сгенерированным `server.js` — брать отдельной задачей с прогоном
   «рестарт под нагрузкой», а не попутно.
6. **Вход через ЕСА** — самый старый висящий мандат (`2026-07-26-esa-login-pull-forward`).
   Упирается в регистрацию OIDC-клиента (класс ② ADR-0010, #116). **23.08
   проверено: клиент не выдан** — письмо-запрос brain
   (`2026-08-23-esa-client-not-issued-request-before-0901`). Как только есть
   `client_id` — одна сессия по рецепту Интера (`trener/web/src/lib/auth/oidc.ts`);
   если `client_secret` окажется только в комнате — vault-клиент по спеке с
   ретраями и `ensure_secret` (#171), не по старой.
7. ~~**Favicon 16 px глазами** (#093)~~ — закрыто 23.08: посмотрен, был
   нечитаем, перегенерирован (см. блок 23.08).
8. **ЕСА/сканер — ответы brain**: после 24.08 ждём подтверждения обхода по
   артефакту; после 01.09 — по ЕСА.

## Ждёт решения владельца

1. ~~**OAuth-токен Метрики**~~ — закрыто 10.08: владелец отказался от блока
   «сегодня / вчера», токен не нужен. ~~Пустой `/etc/vmalmyzhe/.env.u1xhye`~~ —
   снесён 10.08.
2. **Кто ведёт ежедневный отбор новостей.** Инструмент готов ([`docs/PUBLISHING.md`](PUBLISHING.md)), но отбор и привязка медиа требуют глаз — автоматизировать нельзя без ответа на этот вопрос.
3. **Судьба `knip`** — ~~включать в CI или убрать~~ закрыто 05.08: включён в CI как
   гейт `deadcode` (PR #35), мёртвые экспорты почищены.
4. ~~**Токен vault-комнаты**~~ — закрыто 03.08: токен установлен, ingest-ключ
   зеркалирован и подтверждён эхом vault. Grant Сарафану оформлен заранее и должен
   сработать без доступа к его прод-среде.

## Открытые продуктовые вопросы

- Рубрика «Происшествия» впервые наполнена 05.08 (#77573) — следить, чтобы туда не
  попадали «жёлтые» репосты соседних районов.
- Какие предприятия и ИП первыми размещаем в каталоге и рекламе.
- Кто отвечает за ежедневное наполнение новостей, афиши и календаря.
- Финальный список рубрик лучше утвердить по статистике первого живого потока.

## Технические заметки

- **`draft: false` в Payload не публикует.** При `versions.drafts: true` состояние берётся из `data._status`; без него документ остаётся черновиком молча (201, id есть, публичный API отдаёт `totalDocs: 0`).
- **Сообщество публикует сборные дайджесты**: в одном посте несколько сюжетов через «✍» от разных источников, вложения лежат вперемешку. Брать все вложения подряд нельзя — в #77614 фото 3–5 были из сюжета про убийство в Паттайе, в #77631 шестое фото — митинг ВДВ в Калинино. Спорные картинки проверялись просмотром.
- **Повторная доставка того же `vkPostId` перекачивает медиа заново**: после двух прогонов 52 файла вместо 26. Это **не сироты** — на них ссылается история версий (`_posts_v`, `_posts_v_rels`), чистить SQL'ом нельзя, только через админку.
- Локальный `build` требует Postgres; служба `postgresql-x64-17` на машине владельца не стартует без прав администратора. Полная сборка проверяется в `deploy-prod.yml`.
- Реквизиты прод-бокса (хост, SSH-алиас, пользователь, пути, prod-env, адрес шлюза Сетки) — **только** в локальном неотслеживаемом `docs/INFRA.local.md` (D-038, 23.08). В handoff, письма и комментарии кода их больше не писать.
- Новые шрифты — локально в `web/public/fonts/`; фон — `web/public/patterns/ivory-town-emboss.webp`.
- Миграции Payload не генерировать вслепую: перед применением проверять SQL и drizzle-снапшот. Сегодняшние правки миграции **не требовали** — `publishedAt` и `_status` в схеме уже были.
